## 2026-04-24
1. 更新知识库，应禁止更新模型/维度/切片策略等。如果用户要求修改，则后续再加。修改这些维度时，应该把对应的旧chunk全部删除，然后重新切片向量化。
2. 需要添加文档切片后，编辑切片并重新向量化的功能。
3. Agent可以添加用户可加工具（例如生成文档/联网查询/阅读指定飞书文档等等）。同时应该添加Agent默认工具，具体有哪些默认工具应该跟业务挂钩。
4. 实现工具： 飞书文档模板,包含字段[ 模板内容 ]、[ 文件夹 ]、[ 生成/触发规则 ] [ 命名格式 ] [ 文档类型 ] 
5. 结构化模板： 参考飞书文档模板，创建文档类型时，可以设置结构化模板。与保存路径（飞书文件夹）此类型文档在结构化完成且通过审批后，进行向量化并发送到指定目录。

