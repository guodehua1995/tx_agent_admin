## 2026-04-24
1. 更新知识库，应禁止更新模型/维度/切片策略等。如果用户要求修改，则后续再加。修改这些维度时，应该把对应的旧chunk全部删除，然后重新切片向量化。
2. [2026-05-11 完成]需要添加文档切片后，编辑切片并重新向量化的功能。
3. Agent可以添加用户可加工具（例如生成文档/联网查询/阅读指定飞书文档等等）。同时应该添加Agent默认工具，具体有哪些默认工具应该跟业务挂钩。

## 2026-04-27
2. 添加飞书用户申请功能,飞书机器人接收到消息时，先通过一个前置验证函数，判断是否存在与用户表中（通过open_id）。如果不存在则加入到用户申请表中,管理员配置角色/部门后通过申请即可创建用户。

## 2026-04-29 已完成
飞书文档模板细化：  

**前置**
需要开用飞书云文档权限
docs:doc, drive:drive, sheets:spreadsheet, vc:material, bitable:app, moments:moments, docs:document.media:upload, mail:user_mailbox.message:modify
需要将飞书云文档文件夹分享给带有机器人的群组

此功能目的是允许用户通过agent生成与模板格式匹配的飞书云文档。  
包括如下功能:
**前端**:  
### 模板列表: 
    入口：飞书集成/文档模板  
    包含操作： 分页查询、查看、新建、删除。  
    - 分页查询 每页默认10条 可选10 20 30 40 50。支持按命名格式/飞书文件夹id搜索。  
    - 点击列表中的条目可以进行查看操作，查看时可以编辑并保存。  
    - 表单右上侧有新建按钮，点击可以创建模板  
    - 列表条目最后一列为删除按钮，点击后需要用户二次确认是否删除。确认后调用删除接口。  

### 创建模板（支持手写markdown与传入飞书云文档url解析出markdown）:
包含如下字段
    - 文档模板（必填 markdown格式，支持预览时修改）
    - 飞书文件夹id（必填 生成文档保存路径）
    - 描述 (必填 用来告知AI什么情况下调用当前模板)
    - 命名格式（选填 默认ai起名）  
**创建流程：**
    1. 进入文档模板管理页面，点击新建文档
    2. 弹窗：直接创建/导入云文档。如果用户选导入云文档则进行3,否则进行4。
    3. 引导用户输入云文档路径，用户点击提交后，调用后端接口解析云文档。解析成功则携带模板内容跳转到4,将模板内容填写到文档模板输入框内。解析失败则报错提示用户。
    4. 宽表单创建文档模板，左侧为命名格式，飞书文件夹id，描述三个属性。右侧为markdown编辑器，用户可以在线编辑markdown格式的文档模板。
    5. 用户点击取消则返回文档模板管理页面，点击保存则调用后端接口进行存储。  
**此页面应兼容查看/修改功能**  

### 查看模板
    用户在模板列表页面点击模板条目后,弹窗并根据模板id调用后端接口查询模板内容。并填充到页面对应字段中（与创建共用同一个页面）。用户点击提交后，调用后端修改接口，保存成功则关闭弹窗并刷新列表页。否则提示用户后端报错内容。点击取消则关闭弹窗。

### 删除模板
    用户点击删除按钮后,二次弹窗提示用户：“是否确定删除模板【xxxx】?” 用户确认后调用后端删除接口并刷新列表。否则关闭弹窗。

### 创建/修改agent
    页面入口：AI应用/agent管理/创建or编辑
    表单内添加属性： 绑定模板
    打开弹窗时请求后端接口，拉取所有文档模板 名称：id对。用户可以多选文档模板，选择后前端展示名称，提交表单时携带id数组到后端进行保存。

**后端接口**
### 一、文档模板基础 CRUD（5 个）
模板分页列表查询接口
用途：模板列表页分页加载、支持按命名格式 / 飞书文件夹 ID搜索
入参：页码、每页条数、命名格式关键词、飞书文件夹 ID
根据 ID 获取模板详情接口
用途：点击模板条目弹窗查看 / 编辑时，回显所有模板字段内容
入参：模板 ID
新增文档模板接口
用途：新建模板表单提交保存
入参：Markdown 模板内容、飞书文件夹 ID、描述、命名格式
编辑修改文档模板接口
用途：查看 / 编辑模板后提交保存
入参：模板 ID、Markdown 内容、飞书文件夹 ID、描述、命名格式
删除文档模板接口（逻辑删除）
用途：列表页二次确认后删除单条模板
入参：模板 ID
### 二、云文档导入专用（1 个）
飞书云文档 URL 解析接口
用途：用户输入飞书云文档地址后，后端解析并返回对应 Markdown 内容
入参：飞书云文档 URL / 路径
出参：解析后 Markdown、解析成功 / 失败状态
三、Agent 绑定模板关联（1 个）
获取所有模板简易下拉列表接口
用途：创建 / 编辑 Agent 绑定模板弹窗，拉取全量模板「名称 + ID」映射
入参：无
出参：模板 ID、模板名称 数组列表
 
**文档生成工具**
### 工具目标
打造 agent 通用「文档模板匹配 + 自动生成工具」，让 agent 可基于自身绑定的模板，根据用户文档生成需求，自主匹配最优模板、调用模板生成符合格式的飞书云文档，无需人工干预模板选择，衔接现有文档模板管理体系，提升 agent 文档生成效率和格式准确性。
### 核心逻辑
工具初始化：rag_service.py内 chat/chat_stream函数中，构建agent，自动调用接口，查询自身绑定的所有「模板 ID」，再通过模板 ID 查询各模板的完整信息（markdown 内容、飞书文件夹 ID、描述、命名格式），创建tool,并添加到agent中；
需求匹配：agent 接收用户 “生成文档” 需求（如 “生成项目周报”“生成会议纪要”）后，调用该 tool，tool 提取用户需求中的关键词（如 “周报”“会议纪要”），与缓存的模板「描述」进行语义匹配，筛选出贴合度最高的 1-3 个模板（优先匹配描述完全匹配的模板）；
模板选择：若仅匹配到 1 个模板，tool 自动确认该模板；若匹配到多个模板，tool 将模板名称 + 简要描述返回给 agent，由 agent 结合用户需求进一步确认（或按 “匹配度排序” 自动选择最优）；
文档生成：tool 确认目标模板后，调用后端文档生成接口，传入「模板 ID」（获取 markdown 模板内容）、「飞书文件夹 ID」（确定保存路径）、「命名格式」（若有，无则按 AI 起名），生成飞书云文档；
结果反馈：文档生成成功后，tool 返回飞书云文档链接、文档名称给 agent，由 agent 通过飞书机器人（现有逻辑 不需要修改）反馈给用户；生成失败则返回明确错误提示（如 “模板解析失败”“飞书文件夹权限不足”），同步给 agent 转达用户。
### 详细功能需求
（1）agent 与 tool 交互需求
agent 需具备该 tool 的调用权限（绑定模板id视为开启，否则不构建tool）；
agent 调用 tool 时，传递最近10轮对话历史记录,在工具中，由llm判断是否满足生成模板的需要，如果不满足则告知用户当前已经包含哪些内容，仍需用户补充哪些内容。直到满足生成模板需要。之后结合用户历史会话与模板，通过llm生成markdown格式的产出文档，并按用户配置的命名格式命名。然后调用飞书云文档创建api，创建文档到指定目录下；
tool 需向 agent 返回 3 类结果：匹配的模板列表（多模板时）、文档生成成功（含链接 + 名称）、生成失败（含错误原因）。
（2）tool 核心功能需求
模板查询：支持通过「agentID」查询该 agent 绑定的所有模板详情（依赖新增接口）；
语义匹配：支持基于用户需求关键词与模板「描述」进行模糊匹配、精准匹配，匹配度排序（精准匹配＞模糊匹配，可配置匹配权重）；
模板确认：多模板匹配时，提供模板筛选建议（标注匹配关键词），支持 agent 手动确认或自动选最优；
文档生成：调用后端生成接口，严格遵循模板的 markdown 格式、飞书文件夹路径，支持按模板配置的命名格式生成文档名称（无命名格式则按 “模板名称 + 时间戳”AI 起名）；
异常处理：
① 若用户要求生成飞书云文档且agent 未绑定任何模板，返回 “当前 agent 未绑定文档模板，请先在 agent 管理中绑定模板”；
② 若无匹配模板，返回 “未找到与需求匹配的模板，请调整需求描述或绑定对应模板”；
③ 若模板解析失败、飞书文件夹无权限，返回具体错误原因，便于 agent 反馈用户。
（3）后端接口补充需求（基于现有 7 个接口，新增 2 个，不改动原有接口）
新增「根据 agentID 查询绑定模板详情接口」
用途：tool 初始化时，查询该 agent 绑定的所有模板完整信息（含 ID、markdown 内容、飞书文件夹 ID、描述、命名格式）
入参：agentID；出参：模板列表（含所有模板字段）
新增「根据模板 ID 生成飞书云文档接口」
用途：tool 确认模板后，生成实际飞书文档
入参：模板 ID、命名前缀（可选）；出参：飞书文档链接、文档名称、生成状态

**注意：** 你必须了解项目代码结构之后再进行开发，优先使用现有项目结构编写代码。在完成代码后，你需要自行检查代码是否存在语法错误与编译错误。并扫描相关类与文件夹，基于代码可读性与扩展性酌情对新生成的代码与相关老代码进行重构(与本次功能无关代码不在重构范围内)。



## 2026-05-06 已完成
1. 添加工具 网络内容读取 支持agent输入文件url 返回url文字内容。 当文档url前缀为https://my.feishu.cn/docx时,使用现有的飞书机器人读取飞书文档的方式，返回纯文字飞书文档内容。其它url使用开源且可靠的url转文本框架实现。 rag_service.py内 chat/chat_stream函数中，构建agent时默认添加此工具。工具说明简介清晰。
2. 添加工具 获取当前系统时间

## 2026-05-11 已完成
1. [完成]文档类型由纯后端控制,只给前端展示名称和code,根据code路由到不同的函数进行结构化处理。产出markdown格式的文档作为rag文档。
目前只有一个飞书文档 是从飞书拉取普通文档 并通过llm转为markdown
你需要再实现一个ppt转markdown的文档类型 ppt支持上传ppt文件和url（飞书云文档）。将ppt内容按页解析为markdown文档。
解析方式可以为：
将ppt单页转为图片格式->调用llm对图片进行理解->输出以markdown格式输出单页内容。
注意每页直接需要通过一级标题区分。
所有页输出完成后，将每页作为一个llamaindex文档（段）进行向量化存储,超过指定长度的对单页使用markdown切割器进行文本切割。metadata中包含ppt文档路径（参考普通飞书文档），方便召回时展示引文。
2. [完成]添加一个功能,知识库管理页面，点击知识库之后可以进入知识库内容管理页面。展示知识库中关联的所有文档。用户可以在内容管理页面添加/删除文档。删除的文档需要在向量表中一并删除对应数据。在内容管理页面可以点击文档跳转到文档内容管理页面。展示文档的每个切片的文本内容，用户可以添加/修改/删除切片。添加切片后，需要添加到向量表中，并与当前文档/知识库正确关联。修改文本内容时，需要对向量表中的对应内容做重新向量化。你需要先理解需求，整理思路后产出功能说明与技术拆解文档给我。经过我审核/修改/同意之后在依据最终确定的功能说明与技术文档编写代码。

## 2026-05-13
1. 验证下ppt解析的能力
2. 更新知识库，应禁止更新模型/维度/切片策略等。如果用户要求修改，则后续再加。修改这些维度时，应该把对应的旧chunk全部删除，然后重新切片向量化。
3. ppt解析功能有问题 
我刚查了飞书官方资料 飞书开放接口现在不支持导出https://my.feishu.cn/slides/WnqJsCc5ClVCS7ddf1acxfldn0f
这种经过飞书处理的飞书云文档ppt链接
但是支持
https://my.feishu.cn/file/UzkYbMd1coEV42xyo0lczWz1nXb
这种文件格式的ppt文件下载

所以现在我需要你做如下工作
1. 将原有的飞书ppt内容拉取功能做排查 看看都有哪些文件涉及到
2. 实现飞书文件下载功能,当解析出的doc_type=file时，走文件下载。支持扩展名为docx,pdf,pptx
3. 实现文档转换器
    功能描述： 提取文档内容，为后续切片做准备
    入参： 文件流，文件类型（docx,ppt,pdf,png,jpg等常见格式）
    出参： 文档对象数组 包含内容 文本，页码（你可以设计下还有没有其它需要的字段或者直接用llamaindex的document/chunk类型）

    **注意：** 根据文件类型判断处理方式。例如 docx直接获取文字内容即可。ppt/pdf需要先按页处理成图片，然后将图片发送给llm，让llm将其总结为Markdown，作为当页内容。
4. 将文档转换器与飞书文件下载功能结合，当用户上传的飞书云文档url解析为file类型时，下载文件为流（尽量不保存本地文件，如果必须保存则读取完内容要及时删除），并调用文档转换器函数读取文档内容。、
5. 文档转换器是一个独立功能，需要考虑到后期扩展其它文件类型/读取方式/产出文本结构。
现在请你作为产品经理仔细考虑这个需求是否完善，有没有可以扩展和优化的功能或者细节。先以自然语言和我讨论，达成一致后形成标准功能文档给我。


## 2026-05-14
1. ppt解析时，应将其原始文件截图保存在文件服务器上，并生成一条页记录，关联文档id，当前页面页码，页面内容，页面截图路径等。并将页记录放入向量库数据的metadata中。当知识库召回的数据来源为ppt时，应根据文档id与页id查询关联的前后页面一并作为召回结果返回（需要去重）。来保持ppt文档的前后页面连续性。

## 2026-05-15
1. 参考资料：需要增加参考资料类型,bot和前端页面根据不同类型拼接不同的展示方式

## 2026-05-19
1. [完成]在聊天历史记录中,将role改为type,增加类型:tool_call,tool_call_result。用来记录工具调用与工具调用结果。开启新一轮会话时，除了用户输入与ai回复。过往工具调用相关记录也需要添加进上下文中，以保持上下文完整。但是用户展示依旧只展示用户输入与ai回复。
2. [完成]规范metadata:提取现有向量库的metadata中的共有属性为一个类，这样后续增加其它文档时可以按照规范写metadata。但是也要考虑到每种不同类型文档的区别，需要可以扩展。

2. [完成]新功能：合同专家
定位: 合同审批与问答agent
入库流程： web端上传->文档类型为合同->输入url并提交-> 转图片-> 提取完整内容（合同类型的单独函数，使用markdown分割线在不同条款间进行分割） -> 提取元数据（标题、甲方、乙方、合同形式等）->保存文档->走审核流程-> 入向量库
注意：此处进入向量库的有两种类型数据 1. 对于整个文件的综合描述:标题、甲方、乙方、合同形式、合同内容摘要等。用来后期做合同文件搜索使用。 2. 根据条款进行切片，每个条款（根据分割线拆分）作为一个chunk进行向量化和存储。后续用户针对某种条款提问时，可以召回这类切片后的数据让用户参考。两种类型通过metadata中的is_summary区分。

agent构建：
使用llama-index,根据用户输入判断用户意图来调用工具,如果用户查询某个文件，那么就引导用户输入标题、甲方、乙方、合同形式等文件查询参数（只要满足其中几项即可）,然后设置查询参数metadata.is_summary=true并进行查询向量库。依据查询出的合同文档id，查询合同全文并作为参考回答用户问题。
如果用户咨询某类问题,那么则使用metadata.is_summary=false，并提取用户输入中的合同关键词进行查询,依据返回的chunk内容直接回答用户提问。


## 2026-05-25
1. 【完成】切换到云端pgsql和ppt转图片服务
2. 【完成】兼容豆包模型/embdding模型，并成功向量化文档
3. 【完成】修复阿里云qwen3模型，无法获取输出内容的bug（由于思考模式导致，改为非思考模式即可）
4. 【todo】 agnet基础模型改为豆包
 
## 2026-05-26  
1. 【完成】 因agent处理tool/tool_result类型消息 需要记录额外参数，所以添加agent友好的chat_message查询接口，在接口中将工具调用转为ai输出消息。
2. 【完成】 飞书卡片没有正确处理一级/二级/三级标题。飞书不支持#/##/### 已简单替换为# → **粗体**，## → **粗体**，### → *斜体* 如果后续有更多样式要求再改。


## 2026-05-26
1. 【未完成】现在处理切片/原始文档拉取等任务。都是一次性任务,需要建立定时任务补偿机制。这样服务器重启后还能正常运行。（需要建立redis锁，避免开发环境和测试环境竞争。）（需要有一个机制，让某些任务只在本地开发环境定时任务执行，这样能满足debug需要）

## 2026-05-28
1. 【完成】知识库需要可以配置关联召回数量n,命中chunk之后关联召回前n个和后n个相同source_doc_id的chunk
2. 【进行中】配置火山云文件存储,截图上传到火山云,并支持展示 等张亚博时间 需要开ak/sk
3. 【完成】合同类型的每个条款还是不对，需要再调整下。问题1： 当跨页出现时,上一页出现的一级条款/标题就失效了。这个做一个动态切换当前主条款的记忆机制。
问题2： 有些合同是以模板（说明合同条款及定义）+ 附件（说明具体合作形式与具体约定）。这种存在模板在前n页，附件在后n页的情况。此时会出现模板与附件都对同一条款做出说明和约定，但是chunk的条款中两者没有关系的情况。需要考虑下怎么优化。
问题3：合同中存在大量具体数据，例如工时/价格明细/人员清单/物料清单等。这种需要在切片阶段进行处理，具体数据改为简短描述数据类型及作用以及所属条款。不将具体数据作为chunk保存。
4. 知识库-管理内容页面404